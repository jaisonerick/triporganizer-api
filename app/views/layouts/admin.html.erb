<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="ROBOTS" content="NOODP" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <%#
    Configure default and controller-, and view-specific titles in
    config/locales/en.yml. For more see:
    https://github.com/calebthompson/title#usage
  %>
  <title><%= title %></title>
  <%= stylesheet_link_tag :admin, media: "all" %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= body_class %>">
  <% if admin_signed_in? %>
    <nav class="navbar navbar-default navigation-clean">
      <div class="container">
        <div class="navbar-header">
          <a class="navbar-brand navbar-link" href="#">TripOrganizer</a>
          <button class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navcol-1"><span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></button>
        </div>
        <div class="collapse navbar-collapse" id="navcol-1">
          <ul class="nav navbar-nav navbar-right">
            <li role="presentation"><%= link_to 'Viagens', admin_trips_path %></li>
            <li role="presentation"><%= link_to 'Clientes', admin_users_path %></li>
            <li role="presentation"><%= link_to 'Empresas', admin_companies_path %></li>
            <li class="dropdown"><a class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false" href="#"><%= current_admin.name %> <span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <li role="presentation"><%= link_to 'Meus Dados', edit_admin_admin_path(current_admin) %></li>
                <li role="presentation"><%= link_to 'Administração', admin_admins_path %></li>
                <li role="presentation"><%= link_to 'Sair', destroy_admin_session_path, method: :delete %></li>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </nav>
  <% end %>

  <%= render "layouts/admin/flashes" -%>

  <div class="container">
    <%= yield %>
  </div>

  <%= render "layouts/admin/javascript" %>
  <%= render "layouts/admin/css_overrides" %>
</body>
</html>
