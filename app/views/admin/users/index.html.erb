<div class="row">
  <div class="col-md-12">
    <div class="page-header">
      <%= link_to 'Novo Cliente', new_admin_user_path, class: 'btn btn-primary pull-right' %>

      <h1>Clientes</h1>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table">
      <thead>
        <tr>
          <th><%= User.human_attribute_name(:email) %></th>
          <th><%= User.human_attribute_name(:name) %></th>
          <th><%= User.human_attribute_name(:picture_url) %></th>
          <th><%= User.human_attribute_name(:cpf) %></th>
          <th><%= User.human_attribute_name(:birthday) %></th>
          <th><%= User.human_attribute_name(:address) %></th>
          <th><%= User.human_attribute_name(:phone) %></th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @admin_users.each do |admin_user| %>
          <tr>
            <td><%= admin_user.email %></td>
            <td><%= admin_user.name %></td>
            <td><%= admin_user.picture_url %></td>
            <td><%= admin_user.cpf %></td>
            <td><%= l admin_user.birthday %></td>
            <td><%= admin_user.address %></td>
            <td><%= admin_user.phone %></td>
            <td class="text-right">
              <div class="btn-group" role="group">
                <%= link_to 'Editar', edit_admin_user_path(admin_user), class: 'btn btn-default' %>
                <%= link_to 'Remover', [:admin, admin_user], method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-default' %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
