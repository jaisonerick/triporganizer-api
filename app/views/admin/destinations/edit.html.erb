<h1>Editing Destination</h1>

<%= render 'form', destination: @destination %>

<%= link_to 'Show', [:admin, @destination.trip, @destination] %>
<%= link_to 'Back', admin_trip_destinations_path(@trip) %>
