<h1>Editing Boarding Ticket</h1>

<%= render 'form', boarding_ticket: @boarding_ticket %>

<%= link_to 'Show', [:admin, @trip, @registration, @boarding_ticket] %> |
<%= link_to 'Back', admin_trip_registration_boarding_tickets_path(@trip, @registration) %>
