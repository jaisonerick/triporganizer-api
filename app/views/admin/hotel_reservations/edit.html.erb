<h1>Editing Hotel Reservation</h1>

<%= render 'form', hotel_reservation: @hotel_reservation %>

<%= link_to 'Show', [:admin, @trip, @registration, @hotel_reservation] %> |
<%= link_to 'Back', admin_trip_registration_hotel_reservations_path(@trip, @registration) %>
