<div class="row">
  <div class="col-md-12">
    <div class="page-header">
      <%= link_to 'Novo Documento', new_admin_trip_custom_document_path(@trip), class: 'btn btn-primary pull-right' %>

      <h1><%= @trip.name %> </h1>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <ul class="nav nav-tabs">
      <li role="presentation">
        <%= link_to 'Detalhes', [:admin, @trip] %>
      </li>
      <li role="presentation">
        <%= link_to 'Passageiros', admin_trip_registrations_path(@trip) %>
      </li>
      <li role="presentation">
        <%= link_to 'Patrocinadores', admin_trip_sponsors_path(@trip) %>
      </li>
      <li role="presentation" class="active">
        <a href="#">Documentos</a>
      </li>
    </ul>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table">
      <thead>
        <tr>
          <th>Name</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% @custom_documents.each do |custom_document| %>
          <tr>
            <td><%= custom_document.name %></td>
            <td class="text-right">
              <div class="btn-group" role="group">
                <%= link_to 'Editar', edit_admin_trip_custom_document_path(@trip, custom_document), class: 'btn btn-default' %>
                <%= link_to 'Remover', [:admin, @trip, custom_document], method: :delete, data: { confirm: 'Você tem certeza disso?' }, class: 'btn btn-danger' %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
